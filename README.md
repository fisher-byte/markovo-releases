# Markovo Releases

Public release artifacts for Markovo client products.

- **Reader runtime** (): local GPU OCR middleware for `markovo local-convert --reader`
- **CLI wheels** (): thin client wheel for `pip install`
